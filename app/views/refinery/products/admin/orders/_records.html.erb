<%= render 'refinery/admin/search_header', :url => refinery.products_admin_orders_path %>
<div class='pagination_container'>
  <% if @orders.any? %>
    <%= render 'orders' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
